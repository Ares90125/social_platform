import styled from 'styled-components';

export const CampaignGroupWrapper = styled.ul`
  padding: 0;
  margin: 0 auto 36px;
`;

export const GroupDate = styled.p`
  margin: 0;
  color: #393e43;
  opacity: 0.4;
  font-weight: 500;
  font-size: 13px;
  text-transform: uppercase;
`;

export const CampaignListWrapper = styled.ul`
  padding: 0;
  margin: 0;
`;
